export type ButtonProps = {
  title: string,
  data?: string,
  onClickHandler: () => void
}

export type cardTitle = {
  title: string
}
export type contactForm = {
  contactInfo: {
    username: string,
    email: string,
    password: string
  }


}